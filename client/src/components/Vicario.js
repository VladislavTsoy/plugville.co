import React, { Fragment } from 'react';

// Required Components
import Headline from '../shared/Headline'

const Vicario = () => {
    return (
        <Fragment>
            <Headline />
            vicario page
        </Fragment>
    );
};

export default Vicario;