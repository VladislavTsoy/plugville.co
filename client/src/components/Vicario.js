import React, { Fragment } from 'react';

// Required Components
import Headline from './Headline'

const Vicario = () => {
    return (
        <Fragment>
            <Headline />
            vicario page
        </Fragment>
    );
};

export default Vicario;