import React from 'react';

// Required Components
import Headline from './Headline';

const Home = () => {
    return (
        <div>
            <Headline />
            home page
        </div>
    );
};

export default Home;