import React from 'react';

// Required Components
import Headline from './Headline';

const Agency = () => {
    return (
        <div>
            <Headline />
            agency page
        </div>
    );
};

export default Agency;