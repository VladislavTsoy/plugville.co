import React, { Fragment } from 'react';

// Required Components
import Headline from '../shared/Headline';

const Agency = () => {
    return (
        <Fragment>
            <Headline />
            agency page
        </Fragment>
    );
};

export default Agency;